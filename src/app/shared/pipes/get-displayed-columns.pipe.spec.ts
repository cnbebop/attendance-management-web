import { GetDisplayedColumnsPipe } from './get-displayed-columns.pipe';

describe('GetDisplayedColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDisplayedColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
